# Full Codebase Audit Report -- 2026-03-06

## Executive Summary

The codebase is **production-solid**. Architecture, core logic, safety filters, and async patterns are all well-implemented. The main issues are **documentation drift** -- CLAUDE.md, MEMORY.md, and docs/ have fallen behind code changes, particularly around early exit thresholds and config values.

---

## CRITICAL: Documentation Out of Sync with Code

### 1. Early Exit Thresholds -- CLAUDE.md is STALE

CLAUDE.md (lines 188-222) still shows the **old** 4-tier thresholds. The code was updated on 2026-03-05 but CLAUDE.md was not.

| Entry Price | CLAUDE.md Says | Code Does | Config.py Default |
|-------------|---------------|-----------|-------------------|
| < 0.35 | 0.50 | 0.50 (from config) | 0.50 |
| 0.35 - 0.40 | **0.45** | **0.65** (hardcoded) | 0.45 (stale) |
| 0.40 - 0.50 | **0.65** | **0.90** (hardcoded) | 0.65 (stale) |
| >= 0.50 | 0.95 | 0.95 (from config) | 0.95 |

**Three-way mismatch**: CLAUDE.md, config.py defaults, and actual code all disagree for the two middle tiers. The **code is authoritative** (0.65 and 0.90 per MEMORY.md's 2026-03-05 entries).

### 2. MEMORY.md Says WAL Mode is Missing -- It's Not

MEMORY.md audit section lists "no WAL mode" as a known issue. But `storage/db.py` line 30 has `PRAGMA journal_mode=WAL` enabled. **Stale memory note.**

### 3. `/ee` Command Undocumented

main.py line 398 registers `/ee` (early exit tier stats), but CLAUDE.md's command table doesn't list it. CLAUDE.md says "10 commands" but there are actually **12** handlers (including `/ee` and `/spread`).

### 4. CLAUDE.md Command Count Wrong

The Telegram Cleanup changelog entry says "13 -> 10 commands" but the command table actually lists 11 commands (status, stats, trades, recent, today, pause, resume, weights, regime, analyze, spread). Plus the undocumented `/ee` = 12 total.

---

## HIGH: .env.example Massively Stale

`.env.example` is missing **14 settings** that exist in config.py:
- All 4 early exit thresholds
- All regime detection settings (threshold, flip threshold, flip live, confirm windows)
- Streak guard settings
- ML model settings (USE_ML_MODEL, ALWAYS_TRADE, MIN_CONFIDENCE, SIZING_STRATEGY)

Additionally:
- `CONFIDENCE_DAMPEN=0.6` in .env.example but should be `1.0`
- `MIN_EDGE_DOWN=0.08` exists in .env.example but has **no config.py field** (orphan)
- Feature weight mismatches: `W_OBI` (0.10 vs 0.05), `W_REGIME` (0.15 vs 0.20)

---

## MEDIUM: Config Settings Defined But Unused

`config.py` defines `early_exit_threshold_low_mid` (0.45) and `early_exit_threshold_mid` (0.65) but `live_trader.py` **hardcodes** 0.65 and 0.90 instead of reading these values. Either:
- Update config defaults to 0.65/0.90 and wire them into the code, or
- Remove the unused config fields

Also dead code: `polymarket_api_key`, `polymarket_api_secret`, `polymarket_passphrase` -- defined in config.py but never referenced.

---

## MEDIUM: docs/ Files Heavily Stale

| File | Stale Items |
|------|-------------|
| `docs/live-trading.md` | EE threshold still says flat $0.95, MAX_LIVE_BET still $5 |
| `docs/trading-logic.md` | Entry time gate still 120s (should be 60s), exploration range still 0.35 (should be 0.40), EE still $0.95 |
| `docs/ml-model.md` | Dampening still listed as 0.6 (now 1.0) |
| `docs/audit-2026-03-03.md` | Tier 2 #7 (depth double-count) marked OPEN but was fixed 2026-03-05 |
| `docs/plans/*.md` | Multiple design docs still say "Draft" but were deployed; threshold values outdated |

---

## LOW: DB Error Swallowing (Known Tier 2 Bug)

Two methods in `storage/db.py` catch exceptions but don't re-raise:
- `save_skipped_window()` (line 427-428) -- silently drops errors
- `update_live_trade()` (line 600) -- silently drops errors

The `update_live_trade()` one is concerning -- a failed settlement update could leave a trade permanently unsettled with no error surfaced.

---

## LOW: Undocumented Setting

`max_signal_value` (config.py line 12, used in main.py line 146 as `_MAX_SIGNAL_VALUE`) -- skips when any single signal is saturated near +-0.5 limits. Not mentioned anywhere in CLAUDE.md or docs.

---

## What's Done Well

1. **Architecture** -- Clean async separation: data -> signals -> strategy -> alerts. All components properly wired with graceful degradation.

2. **Safety filters** -- All 10 documented filters are correctly implemented and correctly ordered (regime flip before streak guard before trend filter). The time gate, entry price bounds, and one-trade-per-window are solid.

3. **Early exit system** -- The 1-second monitoring loop, tiered thresholds, retry logic, token balance queries, and depth checks are all production-quality. This is genuinely the profit engine and it's well-built.

4. **DB schema** -- Comprehensive with 10 indexes, parameterized queries (no SQL injection), WAL mode enabled, and rich data collection (15 entry-time features, 5 regime components, settlement data).

5. **44-feature ML pipeline** -- Feature count verified, dampening correctly placed in main.py after prediction, model loading with scaler is clean.

6. **Regime detector** -- 5-component weighted composite matches docs exactly, confirmation counter logic prevents flickering.

7. **Error handling** -- Proper try/except throughout, Windows compatibility (signal handler fallback), graceful degradation for optional components (Telegram, live trading, ML model).

8. **Maker fill sync** -- Correctly identifies our fills via `maker_address` matching, properly tagged and excluded from streak guard.

9. **Changelog discipline** -- The reverse-chronological changelog in CLAUDE.md is genuinely useful for knowing which data was collected under which version.

---

## Recommended Actions (Priority Order)

1. **Fix CLAUDE.md EE thresholds** -- Update config section (lines 188-190) and tier table (lines 219-222) to match actual code (0.65 / 0.90)
2. **Fix config.py defaults** -- Either wire `early_exit_threshold_low_mid` / `_mid` into live_trader.py, or update defaults to 0.65/0.90
3. **Add `/ee` to command table** in CLAUDE.md
4. **Fix MEMORY.md WAL note** -- Remove the stale "no WAL mode" claim
5. **Update .env.example** -- Add all 14 missing settings, fix stale defaults
6. **Remove dead config fields** -- `polymarket_api_key/secret/passphrase`
7. **Update docs/*.md** -- Fix stale thresholds, time gate, bet cap, dampening values
8. **Document `max_signal_value`** in CLAUDE.md safety filters section

---

## Detailed Sub-Audit Reports

### A. Config Audit (config.py vs CLAUDE.md)

**Settings in config.py NOT documented in CLAUDE.md:**
- `max_signal_value` (default 0.45) -- used in main.py as `_MAX_SIGNAL_VALUE`

**Dead code fields in config.py:**
- `polymarket_api_key` (line 43) -- never referenced
- `polymarket_api_secret` (line 44) -- never referenced
- `polymarket_passphrase` (line 45) -- never referenced

**Default value mismatches:**
- `VIRTUAL_BANKROLL`: CLAUDE.md says 20, config.py defaults to 100.0
- `CONFIDENCE_DAMPEN`: .env.example says 0.6, config.py and CLAUDE.md say 1.0

### B. Strategy Audit (edge.py + live_trader.py)

**All 10 safety filters verified correct:**
1. Min confidence (0.015) -- correct
2. Entry price filter (0.25-0.65 hard, 0.25-0.40 exploration) -- correct
3. Time gate (60s) -- correct
4. Hour blacklist -- correct
5. Loss streak guard (3 consecutive) -- correct
6. Trend-conflict filter (0.15%) -- correct
7. Regime flip (threshold 0.35, 1-window confirm) -- correct
8. One trade per window -- correct
9. Pause control -- correct
10. Flat close = DOWN (strict >) -- correct

**Filter ordering verified**: Regime flip -> Trend-conflict -> Streak guard (correct per audit fix)

**Early exit logic verified:**
- 4-tier thresholds working (code hardcodes 0.65/0.90 for middle tiers)
- 10s grace period, 20-token min depth, 3 retries with 5s cooldown
- `best_bid_size` used for depth check (not `total_deep_size`) -- fixed 2026-03-05
- Token balance query before selling -- correct

### C. Main.py Orchestrator Audit

**No critical bugs found.** Architecture matches CLAUDE.md exactly.

- All 12 Telegram commands implemented and functional
- Window lifecycle correct (settlement before new market discovery)
- Dampening properly applied (currently disabled at 1.0)
- Regime flip runs BEFORE streak guard + trend filter (audit fix verified)
- Early exit 1-second polling confirmed
- Stats interval 60 minutes confirmed
- Maker fill sync correctly isolated and tagged
- All hardcoded values are timing/infrastructure only (no business logic)
- Graceful degradation for ML model, Telegram, live trading, Chainlink

### D. Data Layer + Signals Audit

**All 7 files pass:**
- `data/models.py` -- All 10 dataclasses correct
- `data/binance_ws.py` -- kline_1m, depth20@100ms, aggTrade, futures funding all present
- `data/polymarket.py` -- Chainlink RTDS stream, batch+single format, 120s buffer
- `signals/indicators.py` -- 8 indicator families (RSI, VWAP, BB, EMA, ATR, momentum, vol z-score)
- `signals/features.py` -- 12-field FeatureVector correct
- `signals/ml_probability.py` -- 44 features verified, dampening in correct location (main.py)
- `signals/regime.py` -- 5-component weighted composite, correct thresholds and classification

### E. Storage Audit (db.py)

**Schema completeness: PASS** -- All documented columns present:
- `live_trades`: 24+ columns including 15 entry-time features, 5 regime components
- `paper_trades`: Core + spread + tag + regime columns
- `skipped_windows`: 20 fields (8 core + model_side + 11 features)
- `market_snapshots`: Core + spread + depth columns

**SQL injection: PASS** -- All queries parameterized with `?` placeholders

**WAL mode: ENABLED** (line 30) -- contradicts stale MEMORY.md note

**Error swallowing (Tier 2 bug):**
- `save_skipped_window()` (line 427-428) -- catches but doesn't re-raise
- `update_live_trade()` (line 600) -- catches but doesn't re-raise

**Indexes: GOOD** -- 10 indexes cover common queries

**Gamma resolution columns**: Added externally via `backfill_gamma_resolution.py`, not in db.py migrations (by design)

### F. Telegram Audit

**All documented commands verified working:**
- Paper trades excluded from Telegram -- confirmed
- Stats interval 60 minutes -- confirmed
- Skip notifications implemented -- confirmed
- Trade alert is single combined message -- confirmed
- `/stats` shows all documented fields -- confirmed
- `/recent [N]` with default 5, max 10 -- confirmed
- `/today` shows count, W/L, P&L, best/worst -- confirmed

**Settlement alerts: WORKING** -- `send_live_settlement_alert()` called at main.py:1991 (initial investigation suggested otherwise, but verified as present)

**`send_settlement_alert()` (paper): Unused** -- defined but never called (correct per design: paper trades excluded from Telegram)

### G. docs/ Staleness Report

| File | Line(s) | Issue |
|------|---------|-------|
| docs/live-trading.md | 13, 101 | EE threshold still says flat $0.95, should be tiered |
| docs/live-trading.md | 35 | MAX_LIVE_BET still $5, should be $10 |
| docs/trading-logic.md | 56 | Exploration range 0.25-0.35, should be 0.25-0.40 |
| docs/trading-logic.md | 56 | Core range 0.35-0.65, should be 0.40-0.65 |
| docs/trading-logic.md | 56-57 | Time gate 120s, should be 60s |
| docs/trading-logic.md | 33 | EE still says $0.95, should be tiered |
| docs/ml-model.md | 64 | Dampening listed as 0.6, now 1.0 |
| docs/audit-2026-03-03.md | 41-45 | Tier 2 #7 marked OPEN, was fixed 2026-03-05 |
| docs/plans/2026-03-02-adaptive-early-exit-design.md | 62 | 3-tier thresholds, now 4-tier with different values |
| docs/plans/2026-03-02-adaptive-early-exit-plan.md | 5, 26 | Threshold values outdated |
| docs/plans/2026-03-03-trend-protection-design.md | 1 | Status "Draft -- pending approval" but deployed |
| docs/plans/2026-03-03-trend-protection-plan.md | 161, 289 | REGIME_FLIP_CONFIRM_WINDOWS=3, now 1 |
